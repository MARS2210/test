@model IEnumerable<WebApplication6.Models.Subjects>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Number_of_class)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name_of_subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hours_per_week)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Number_of_class)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name_of_subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hours_per_week)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSubject }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSubject }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSubject })
        </td>
    </tr>
}

</table>
<p>
    @Html.ActionLink("Back to home page", "Index", "Home")
</p>